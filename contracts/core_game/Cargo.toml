[package]
name = "core_game"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.0"

[dev-dependencies]
soroban-sdk = { version = "20.0", features = ["testutils"] }
